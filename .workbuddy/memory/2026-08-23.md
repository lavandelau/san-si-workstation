# 2026-08-23

## 少年学习空间站 · 第2轮迭代：错题重练模式
- 通读 `少年学习空间站.html`（约3630行）全部代码，掌握结构：十大板块、localStorage 状态（STORAGE_KEY: wb_xuexi_station_v1）
- 用户选定迭代方向：**错题重练模式**（未选：语文基础、学习报告、英语听力）
- 新增内容：
  - 错题重练引擎（~250行）：智能重练 / 知识点定向练习 / 复习卡片自评
  - `knowledgeTypeMap`：中文知识点名→genMathProblem题型映射（含练习卷产生的"三位数除以一位数"等名称，"三下复习"→随机池）
  - 错题列表筛选（待消灭/已掌握/全部）、mastered/wrongCount 字段、redoStats 统计
  - 旧数据兼容：无 mastered 字段视为待消灭
- 测试：Node 提取内联脚本 + mock DOM 跑 37 项功能断言全通过（语法 node --check 通过）；redoStats 在 finishRedo（1.5s 定时器后）累计，已验证
- 已知小边界：最后一题答对后 1.5 秒内点"退出重练"，该回合不计入 redoStats（有 redoSession===s 守卫，无副作用）
- 工具坑：node -e 里 eval(script) 后无法访问其中 const/let（eval 作用域），须把 mocks+script+tests 拼成文件再跑
- 待办候选（用户未选，可能后续要）：语文基础板块（四上生字词/日积月累）、学习报告增强、英语听力句型

## 少年学习空间站 · 第3轮迭代：除法专项改填空矩阵
- 用户需求：数学王国「除法专项」不做选择题，改成填空题、题量多、矩阵式排布
- 实现：`renderDivisionDrill()` 整组 20 题（三位数÷一位数整除，divDrill 数组），4列 CSS 网格（≤900px 3列、≤600px 2列）；回车逐题判分跳格 + 「检查答案」一键判分（未答按错处理显示正确答案）+「再来一组」
- 判分：答对 +1⭐ 计入 progress.math；答错/未答自动 addErrorAuto（知识点"三位数除以一位数"→可被错题重练识别再生成）
- 修正原 genMathProblem 的隐性问题：新出题器保证被除数必为三位数（ceil(100/b)~floor(999/b)）
- 测试：mock DOM 跑 23 项断言全通过；首轮 2 个 FAIL 均为测试 harness 问题（mock 元素不随 innerHTML 重建、期望值算错），非应用缺陷

## 少年学习空间站 · 第4轮迭代：除法矩阵分页（每页10题）
- 用户需求：一页不要 20 题，10 题就好，其余翻页
- 实现：DIV_TOTAL=20 / DIV_PAGE_SIZE=10 / divPage；renderDivisionPage() 只渲染当前页 10 格 + 翻页器（上一页/下一页/第x/2页，边界禁用）；oninput 把答案存进 p.val（跨页判分数据源）；判分核心抽成 gradeDivProblem(i,val) 不依赖 DOM 存在（其他页无元素）；checkDivisionAll 遍历全部 20 题用 p.val 判分后重绘当前页；页尾回车 divInputKey 自动 divGoPage+聚焦；已判题翻页回来按 p.checked/p.right/p.skip 恢复状态（readonly+✓/✗/未答标记+回填答案）
- 测试：27 项断言全通过
- **重要工具坑**：bash 嵌套转义不可靠——mock 正则里的 \d 经 bash+模板字符串双层处理后反斜杠被吞（文件里变成 d+）。多轮"FAIL"其实是 harness 假失败。经验：在 bash -e 内嵌 JS 测试代码时，正则一律用 [0-9] 等无反斜杠写法；怀疑应用 bug 前先打印生成文件的实际字节

## 少年学习空间站 · 第5轮迭代：英语朗读节奏 + 单词矩阵 + 古诗左栏列表
- 用户需求：①字母巩固中字母和单词发音间隔太短 ②单词分类一列一个太浪费，要矩阵 ③诗文花园古诗列表放左侧小列表，不单独成行
- 实现：
  - `speakLetter(letter, word)`：字母慢速(0.7)读完后 onend→800ms 停顿→再读单词(0.75)；含 1400ms 兜底定时器 + wordSpoken 防重；26字母卡和元音卡均改用
  - 单词分类 bug 修复：`#wordGrid` 容器漏挂 `word-grid` 网格类导致一列一个，补上后桌面 auto-fill 140px 多列、手机 ≥2 列
  - 诗文花园：`.poetry-layout` 从内联样式移到 CSS 类（桌面 210px+1fr、列表项紧凑 13.5px、#poemList max-height 520px 滚动）；≤768px 从强制单列改为 104px 迷你列表+右侧诗文两栏（列表项 12px、卡内边距收紧）
- 测试：23 项断言全通过（speakLetter 时序、word-grid 渲染、poetry 布局 CSS 与渲染/切换）
- **工具坑**：Write 工具写的临时测试文件在 bash（沙箱外提权执行）环境不可见，需用 bash heredoc 直接生成测试文件；另注意 `cmd; rm ...` 的分号会在失败时也执行删除，误删过测试文件

## 少年学习空间站 · 第6轮迭代：扩展三大新模块（语文阅读站 + 成语百宝箱 + 阅读打卡）
- 用户需求：以现有空间站为基准继续扩展
- 新增三大模块（导航从10项增至13项）：
  1. **语文阅读站**（`section-reading`）：6篇短文阅读理解（小松鼠的秋天、奇妙的影子、老槐树下的故事、我是小小统计员、小水滴的旅行、学会节约用水），每篇6-7道选择题；练习模式+统计模式两个tab；完成记录存入 readingProgress，错题自动进错题本；全对+5⭐/完成+3⭐
  2. **成语百宝箱**（`section-idiom`）：19个成语（守株待兔、亡羊补牢、刻舟求剑等），三个tab：图鉴（点击看释义+故事+朗读）、故事猜成语（读故事选成语）、成语填空闯关（选字填空）；答对+2⭐，错题进错题本
  3. **阅读打卡**（`section-booklog`）：课外阅读记录（书名/页数/时长/5星评分/读后感），SVG环形进度展示本月打卡进度（目标20次），每次打卡+3⭐
- 系统集成：
  - state 默认值新增 reading/idiom/booklog progress 字段 + books[]/readingProgress{}/idiomProgress{}
  - 新增3个徽章：阅读之星📚(15题)、成语达人💎(15题)、书香少年📖(5次打卡) + 阅读冠军🏆(15次)
  - 每日任务从5项增至6项（新增"语文阅读理解（1篇）"），进度条动态用 dailyTaskDefs.length
  - 闯关冒险新增 reading + idiom 两种题型（从4种增至6种混合出题）
  - 今日学习路线从5步扩展至8步，加入阅读和成语环节
  - 新增3个挑战目标（阅读全对/成语猜对8题/阅读打卡）
  - 诗文花园新增6首三年级经典古诗（望庐山瀑布/山行/赠刘景文/夜书所见/望天门山/饮湖上初晴后雨），grade:'三'排在四年级新诗后面
  - 思维题从27题增至39题（新增12题）
- CSS：新增阅读理解、成语、阅读打卡三组样式 + 移动端响应式
- 语法检查：Node --check 通过（4224行脚本无错误）
- 文件总行数从~4161增至~5049行

## 少年学习空间站 · 第7轮迭代：录音麦克风异常处理（2026-08-24）
- 用户需求：录音异常处理四点——①点击按钮才请求麦克风 ②权限拒绝弹醒目提示（含微信指引文案）③区分找不到设备/权限拒绝/设备被占用三种文案 ④页面顶部常驻浅黄色提示条
- 实现：
  - 确认/保持：getUserMedia 唯一调用点在 startRecite（按钮 onclick 触发），页面加载不发起权限请求；initMicBanner IIFE 只读 UA 不碰权限 API
  - 新增 `showMicAlert(html)`/`closeMicAlert()`：复用 modalOverlay/modal 弹窗（原先 JS 从未用过该 modal，现加上点击遮罩关闭），红色醒目标题+分行文案+「我知道了/刷新重试」双按钮
  - 新增 `micErrorType(e)` 分类（denied/notfound/busy/unknown）+ `showMicError(e)` 四套文案；NotFoundError/NotAllowedError(含旧名PermissionDeniedError)/NotReadableError(含TrackStartError) 均按 DOM 规范名+旧版Chrome名兼容
  - startRecite 前置检查（非安全上下文/不支持MediaRecorder）也改为醒目弹窗；微信 UA 检测（micromessenger）下直接弹「微信内无法录音」引导，不发起权限请求
  - `<main>` 顶部常驻 `.mic-banner` 浅黄色提示条（📢），微信内自动加 `.wechat` 类变红色加强并给出「···→在浏览器打开」指引
- CSS：.mic-banner（浅黄 #FFF8E1/#F5A623 左边框，wechat 红色变体）、.mic-alert-title/line/divide/tip
- 测试：Node --check 语法通过；确认 script 内 getUserMedia 仅1处实际调用

