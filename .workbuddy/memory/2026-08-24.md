# 2026-08-24

## 少年学习空间站 · 第8轮迭代：录音逻辑再优化（https 强制 + 完整异常捕获）
- 用户四点需求：①点击才请求麦克风（已满足，复核确认）②完整捕获异常并区分 NotAllowedError/NotReadableError/NotFoundError ③NotReadableError 用指定文案（钉钉/腾讯会议/蓝牙占用、重新配对、新开标签页）④禁 file:// 协议，录音必须 https 环境
- 实现：
  - 新增 `micEnvCheck()`：`location.protocol === 'https:'` 才放行；file:// 或 http:// 弹【录音启动失败】提示部署到 https（其他功能不受影响）。替换原 `window.isSecureContext` 检查（file:// 属 secure context 会被放行，不符合新需求）
  - NotReadableError（busy）弹窗改为用户指定的三条文案；NotFoundError 标题改【未检测到麦克风硬件】
  - MediaRecorder 创建/启动包进 try/catch（失败即 teardownRecite 释放流 + showMicError）；新增 `reciteRecorder.onerror` 处理录音中途设备断开/被抢占
  - 顶部常驻提示条、微信加强版、录音按钮下方 hint 均补充"需 https 环境"说明
- 测试：mock DOM 跑 15 项断言全 PASS（异常分类含旧版异常名、三类弹窗文案关键词、file:// 拦截/https 放行）；node --check 语法通过
- 工具坑复用经验：提取代码块做测试时边界要落在完整语句上（上次从注释中段切开导致 `=====` 语法错误），用 `function isWeChat` / `let reciteStream` 作锚点

## 少年学习空间站 · 第9轮迭代：关闭录音打分模块
- 用户反馈：浏览器下录音打分仍无法使用，要求关闭「背诵挑战 · 录音打分」
- 实现（总开关方案，可随时恢复）：
  - 新增 `const RECITE_ENABLED = false;`（位于 MIC ERROR HANDLING 段）
  - `initMicBanner`：关闭时 `banner.style.display='none'` 隐藏顶部提示条
  - `renderRecitePanel`：关闭时显示"本功能已停用，请用古诗填空背诵"，不再渲染录音按钮
  - `startRecite` 开头加 `if(!RECITE_ENABLED) return;` 保险锁
  - 挑战目标里"背诵录音拿到100分"改为"把一首新诗背到90分"
- 验证：node --check 通过；RECITE_ENABLED/banner 隐藏/守卫代码均在
- 以后若部署到 https 想恢复录音功能，把 RECITE_ENABLED 改为 true 即可

## 少年学习空间站 · 第10轮迭代：宠物小屋全面重做
- 用户需求：领养5种宠物+取名+蛋孵化、三维状态（饱食/心情/活力，低状态只停经验不惩罚）、任务赚金币+商店消费、等级形态（幼崽→少年→成年）、徽章、防沉迷（每日限时10分钟强制返回主页+当日未完成3项任务禁入）、本地存储、纯H5
- 实现：
  - state.pet 全新结构（adopted/species/hatched/hatchTaps/satiety/mood/energy/inventory/decor/room/lastDecay/interactSec/lastNap）+ state.coins；带旧数据迁移（未使用过的旧默认宠物重置回领养流程，用过的保留名字等级）
  - 流程：门槛(3项任务)→领养(5种SVG宠物:猫/兔/狐/龙/狗)→点蛋3次孵化→Lv1幼崽；等级1-2幼崽/3-4少年/5成年（SVG配项圈/皇冠+尺寸）
  - 金币：每完成1项每日任务+5，全天全完成再+20（改 toggleDailyTask）；商店9种商品（3食物/3玩具/3房间装扮）只能金币购买
  - 喂食/玩耍消耗背包道具恢复三维状态，状态全≥30才得经验；"让它休息"免费回活力（10分钟冷却）；每日状态自然衰减(饱-15/心-10/活-10)
  - 防沉迷：renderPet 时 startPetTimer 每秒累计 interactSec（每10秒落盘），≥600秒 showToast+switchSection('today') 强制返回；switchSection 开头统一 stopPetTimer
  - 新徽章：初见伙伴/最佳主人/金币小富翁/爱心守护者(连续14天 petStreak)
  - UI：大按钮(52px高)、顶部横幅"好好学习，攒金币照顾你的小伙伴！"、宠物上下浮动/蛋摇摆/popIn 轻动画、柔和渐变房间背景
- 旧函数 petSkins/getPetSVG/selectPetSkin/feedPet 全部删除，导航"宠物乐园"→"宠物小屋"
- 测试：mock DOM 34 项断言全 PASS（门槛/金币/领养/孵化/购买/低状态停经验/升级/装扮切换/600秒强制返回/金币不足拒绝/连续打卡/每日衰减/休息冷却 + 旧数据迁移2场景）；node --check 通过


