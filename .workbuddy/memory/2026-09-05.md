# 2026-09-05

## 把「古诗填空背诵」的答错暂停推广到英语/数学点选题
- 机制：判分后锁定其余选项（.locked）+ 输入框 disabled，显示「我知道了，下一题 ➡️」，不再 setTimeout 自动跳走；答对在非计时模式也等孩子点「下一题」。
- 改动点（均在 少年学习空间站.html 源文件）：
  - `checkMathAnswer`：新增 `mathAnswerLocked` 防重点 + 其余选项 locked；新增 `mathNextStep(type)` 统一路由（复习/计时/插旗练习）；`nextMathProblem`/`nextReviewProblem` 重置锁
  - 计时挑战例外：答对仍 700ms 自动连答（保速度手感），答错暂停
  - `checkSpell`：新增 `spellLocked`，答完禁用输入，拼错展示正确拼写+中文并自动记错题本
  - `matchClick`：新增 `matchPaused`，配错红框保持、提示「点任意卡片继续」；全部配对后改为「下一轮 ➡️」按钮（原 2s 自动）
  - `checkRedoChoice`：新增全局 `redoNextStep()` 取代 1.5s 自动推进
- 验证：新增 `_check_pause.js`（vm + DOM 桩，34项断言全通过），`_check_games.js` 回归通过。

## 踩坑记录（重要）
- `build_app.py` 的 `main()` 开头执行 `shutil.rmtree(app)` 再 `makedirs(icons)`，所以「先备份 icons 再 monkeypatch make_icons」的写法无效：main 已重建空 icons 目录，patched 函数判断 `isdir` 为真而跳过恢复，导致 git 已跟踪的 5 个图标被删除。
- 处理：用 `git checkout -- app/icons/` 恢复；随后在受管 venv（`C:\Users\aimee\.workbuddy\binaries\python\envs\default`）安装 Pillow 12.3.0，直接用 `build_app.py` 完整重建（图标正常生成）。
- 今后重建命令：`C:/Users/aimee/.workbuddy/binaries/python/envs/default/Scripts/python.exe build_app.py`（系统 python 无 Pillow）。
- 已同步重建 `app/`（PWA 站点），sw.js 缓存版本随之更新，手机上刷新即可拿到新逻辑。
