# 2026-08-26

## 少年学习空间站 · 第11轮迭代：宠物换成用户提供的 3D 狗狗图
- 用户给出 `3d-dog.png`（豆包AI生成的 3D 渲染腊肠犬，有"小红书与豆包AI生成"水印），要求"宠物使用我发给你的动画"
- 实现：
  - 用 Pillow 把原图 1728×2304 裁掉底部 14% 去水印，再 resize 到 520×596，base64 嵌入 HTML（318KB → 425KB base64 串，文件总体 290KB→660KB）
  - 删掉旧 `petSVG()` 的纯色卡通 SVG 实现，改写为 `petDogHTML(opts)`：返回 `<img>` 包装 + 阶段配饰（少年加脖子光圈 / 成年加皇冠 SVG + 四颗闪光 ✦）
  - 加 6 套 CSS 动画：petBob（上下浮动·保留）、petBlink（每 5.5s 亮度压一下模拟眨眼）、petWag（整图微旋转·模拟尾巴/呼吸）、collarPulse（光圈呼吸）、crownFloat（皇冠上下浮）、sparkleShow（闪光闪动）。物种选择卡片里关掉动画避免闪烁
  - `petSVG(speciesId, opts)` 保留为薄壳→`petDogHTML(opts)`，所有旧调用点零改动
  - 物种列表重命名为腊肠犬颜色变种：奶黄/焦糖/栗子/可可/棕糖（之前是小猫/兔子/小狐狸/小龙/小狗，避免选了猫却看到狗的违和感）
  - 蛋孵化完成加 `burstConfetti()` 撒花特效（纯 DOM/CSS 实现，24 片彩色碎片向四周飞散）
- 验证：
  - Node `--check` JS 语法通过
  - Node 模拟 document/window 跑 petDogHTML 三阶段断言：stage1 无光圈/皇冠、stage2 有光圈无皇冠、stage3 有光圈+皇冠+4 闪光 ✓
  - 旧 `petSVG('cat',{size:88,stage:1})` 兼容调用正常 ✓
  - base64 解码 roundtrip：425120 字符 → 318838 字节，与原 PNG 文件字节级一致 ✓
- 工件：`apply_pet.py`（完整改动脚本，含 base64 嵌入、CSS 替换、物种改名、撒花特效，可保留供后续回滚/参考）
- 文件大小：HTML 从 290KB → 660KB（base64 狗狗图占 425KB）；离线单文件属性不变

## 少年学习空间站 · 第12轮迭代：宠物三态+修复领养锁屏bug
- 用户反馈：
  1. 希望狗狗有「坐/走/卧」三种动作
  2. 领养流程中错误弹出「宠物小屋还没开门哦」
- 修复 bug：
  - `petGateOk()` 增加「已领养用户不再被锁」逻辑，避免领养流程中由于 `dailyTasks.completed` 不同步而再次触发锁屏
  - `renderToday()` 中把计算出的 completed 实时同步到 `state.dailyTasks[today].completed` 并 saveState，防止其他模块读到旧值
- 新增三态图片：
  - 基于坐姿原图用 ImageGen（图生图）生成走姿、卧姿各一张，再统一裁剪底部 12% 去水印
  - 三张图都 resize 到 400px 宽、JPEG 质量 85 压缩后 base64 嵌入，总体积从单张 PNG 425KB base64 降到三张合计约 57KB base64
  - HTML 文件大小从 660KB 回降到 351KB
- 宠物姿态逻辑：
  - `PET_DOG_IMG = {sit, walk, lie}` 替代原来的 `PET_DOG_SRC`
  - `petDogHTML(opts)` 增加 `pose` 参数，并支持 `state.pet._tempPose` 临时覆盖
  - `petCurrentPose()` 自动判断：活力低/心情差 → 卧；心情好且活力足 → 走；默认 → 坐
  - 喂食/玩耍后临时切「走」1.5s，点「休息」后临时切「卧」2s
- 文件状态：保留 `3d-dog.png`（坐姿原图）、`apply_three_poses.py`（三态脚本）；中间大图和临时文件已清理

## 少年学习空间站 · 第13轮迭代：去掉狗狗浮动动画
- 用户反馈：宠物乐园里的小狗图不要上下浮动
- 改动：
  - 删除 `@keyframes petBob` 和 `.pet-bob` CSS（整图 translateY 上下浮动）
  - `petDogHTML()` 里去掉 `bob`/`pet-bob` class 的拼接逻辑
  - 保留 petBlink（眨眼亮度）、petWag（微旋转）、光圈/皇冠/闪光等效果未动
- 验证：grep 无 pet-bob/petBob 残留；`node --check` JS 语法通过

## 少年学习空间站 · 第14轮迭代：去掉狗狗左右摇摆
- 用户反馈：去掉 petBob 后小狗图仍在左右轻微摇晃
- 原因：`petWag` 动画会把整张狗狗图 ±1.6° 旋转，看起来像左右晃
- 改动：删除 `@keyframes petWag` 及 `.pet-stage-1/2/3 .pet-dog-img` 里的 `petWag` 动画引用
- 保留：petBlink（亮度眨眼）、少年光圈、成年皇冠/闪光效果
- 验证：`grep petWag` 无残留；`node --check` JS 语法通过

## 少年学习空间站 · 第16轮迭代：修复「闯关：闯关模式连闯3关」无后续动作的 bug
- 用户反馈：选了「闯关：闯关模式连闯3关」目标后没有任何后续动作，文本只是存进 state.goals
- 改造：
  - `defaultState()` 增加 `goalCompleted:{}` 与运行期 `state.advGoalBase = {date, level}`（记录达成目标前的起始累计关数）
  - `pickGoal()` 查找 goalQuickDefs 中对应 cat，cat==='闯关' 时 toast 后调用 `switchSection('adventure')` + `setTimeout(startAdventure, 200)`，真正跳进闯关乐园自动开打
  - HTML 在闯关乐园卡内「开始闯关」按钮上方加 `#advGoalProgress` 提示条
  - 新增 `renderAdvGoalProgress()`：根据今日目标是「闯关模式连闯3关」时显示进度 X/3 或已达成标记；`renderAdventure()` 调用
  - `checkAdvAnswer()` 在每闯过 1 关（advLevel++）后调用 `maybeFinishAdvGoal()`：到 3 关即标记 `state.goalCompleted[today]=goal`、额外奖励 20 颗星、toast 庆祝
  - `updateGoalDisplay()` 当目标是闯关时显示「今日闯关进度 N/3 关」或「已达成」
- 验证：JS 语法通过

## 少年学习空间站 · 第15轮迭代：宠物小屋加入坐走卧循环动画
- 用户提供了一段 sitWalkLie keyframes 演示代码（单图 + translateY/scaleY 伪装姿态 + 远程 byteimg 图片链接），要求加进宠物小屋
- 适配改造（保持离线单文件原则）：
  - 远程图片链接替换为已内嵌的坐/走/卧三张 base64 真图，用 opacity 轮播而非单图缩放伪装
  - `petDogHTML()` 增加 `cycle` 选项：渲染三层堆叠 `<img>`（.pd-sit/.pd-walk/.pd-lie）
  - CSS：`pcSit/pcWalk/pcLie` 三组 12s opacity 关键帧（坐 0-30% → 走 30-60% → 卧 60-90% → 回坐），`petCycleWalk` 在走路段做 ±14px 踱步位移
  - `petMainHTML` 主展示调用改为 `cycle:true`；`_tempPose`（喂食/玩耍/休息临时姿态）优先于循环模式
  - 物种选择卡片等其他调用点保持单图静态渲染，不受影响
- 验证：cycle 模式输出含三张图；普通模式单图；临时姿态覆盖循环；`node --check` 通过